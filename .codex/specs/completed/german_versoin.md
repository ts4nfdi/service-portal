Task:
- Create a German version of the application.

Details:
- Here you are allowed to use next-intl plugin
- Put the translations for each component besides it in a same directory.
- common text must go a shared place and reused instead of duplicating it.
- there must be a language switcher in the header besides login button.
- default language is English.
- second language is German.
- language chagne must be reflected in the url.
- App functions must be the same. Do not change the app logic.
- translate all static texts and json files (they should be available in both languages).
  - Expection: publications.json
- use dropdown and flags for language switcher.


